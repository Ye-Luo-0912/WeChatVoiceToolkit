# First-round handoff

Validate restore/build, add bounded low-level tests, and run `doctor`,
`snapshot create`, `schema probe`, and helper `ping`. Do not implement a
schema adapter, UI, key scanning, key extraction, or decryption until the user
provides verified schema data and version context.
